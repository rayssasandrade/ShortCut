@model SisGerenciador.src.Models.PreRequisito

@{
    ViewData["Title"] = "Create";
}

<h1>Novo</h1>

<h4>PreRequisito</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="LiberadaId" class="control-label"></label>
                <select asp-for="LiberadaId" class ="form-control" asp-items="ViewBag.LiberadaId"></select>
            </div>
            <div class="form-group">
                <label asp-for="LiberadoraId" class="control-label"></label>
                <select asp-for="LiberadoraId" class ="form-control" asp-items="ViewBag.LiberadoraId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Criar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Voltar a lista</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
